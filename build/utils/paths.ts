import { resolve } from 'path'

export const ROOT_PATH = resolve(__dirname, '..', '..')
export const BUILD_PATH = resolve(ROOT_PATH, 'build')
export const SRC_PATH = resolve(ROOT_PATH, 'src')
export const LIB_PATH = resolve(ROOT_PATH, 'lib')
export const ESM_PATH = resolve(ROOT_PATH, 'es')
export const DIST_PATH = resolve(ROOT_PATH, 'dist')
export const TYPE_PATH = resolve(ROOT_PATH, 'typings')
export const SCRIPT_PATH = resolve(ROOT_PATH, 'scripts')
